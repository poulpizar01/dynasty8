node_modules/
*.log

# Pack VPS (deploy/vps) : secrets réels et sauvegardes, jamais dans Git
deploy/vps/.env
deploy/vps/backups/
Claude outputs/
_a-envoyer/
# Comptes rendus des scripts (migration / nettoyage des photos)
rapports/
# Sauvegarde de la base à restaurer par l opérateur (choix de Thomas : dans le dépôt)
!deploy/operateur/*.dump
!deploy/operateur/.env

